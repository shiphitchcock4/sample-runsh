language: node_js

node_js: 0.12

build:
  ci:
    - echo "Hello, World!"
    - echo "This is master"
    - echo "This is ??????"
    - echo "Hello, World!"
    - docker ps
    - echo "<==========================>"
    - docker ps -a
    - docker images
    - ifconfig
    - printenv
